#!/usr/bin/env python
# -*- coding: utf-8 -*-

class gender_classifier():
    def tokenize_line(self, line):
        for word in nltk.word_tokenize(post):
    def extract_stopwords(self, features):
        reduced_features = nltk.corpus.stopwords.words('english')
        return reduced_features

    def feature_extractor(self, line):
        tokenized = self.tokenize_line(line)
        features = self.

    def __init__(self, line):
        features = feature_extractor(line)
        print features
